<UserControl x:Class="Color_Generator.Views.DemoModal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Color_Generator.Views" 
             xmlns:viewmodels="clr-namespace:Color_Generator.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:DemoModalViewModel}"
             mc:Ignorable="d" Width="300" Height="200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Text="Modal" FontSize="20" VerticalAlignment="Center" TextAlignment="Center" Foreground="Red" />
        <Button Grid.Row="1" Command="{Binding CloseModalCommand}" Content="Close" />
    </Grid>
</UserControl>
